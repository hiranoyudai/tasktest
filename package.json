{
  "name": "simple-task-manager",
  "version": "1.0.0",
  "private": true,
  "description": "A simple static task management app with local persistence.",
  "scripts": {
    "start": "python3 -m http.server 5173",
    "build": "rm -rf dist && mkdir -p dist/src && cp index.html dist/ && cp src/main.js src/styles.css dist/src/"
  }
}
